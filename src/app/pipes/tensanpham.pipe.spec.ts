import { TensanphamPipe } from './tensanpham.pipe';

describe('TensanphamPipe', () => {
  it('create an instance', () => {
    const pipe = new TensanphamPipe();
    expect(pipe).toBeTruthy();
  });
});
